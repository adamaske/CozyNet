#include <iostream>

void Parser(int payload) {
	std::cout << "PARSER : " << payload << "\n";
}

int main() {

	std::cout << "Hello World!\n";
	
	//Server server;
	//server.name = "CozyServer";
	//server.
	//server.AddPayloadParser(Parser);
	//server.Start();

	//Client client;
	//client.Connect();
	//while (client.IsConnected())

	//if(input){ 
	//	server.Stop();
	//}
	return 0;
}